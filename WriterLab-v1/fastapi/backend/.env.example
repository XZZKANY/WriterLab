# 后端必填：PostgreSQL 连接字符串。推荐使用 psycopg3：postgresql+psycopg://...
DATABASE_URL=postgresql+psycopg://USER:PASSWORD@localhost:5432/writerlab

# 可选：把 SQLAlchemy SQL echo 打开（默认关闭）；调试 SQL 时再设为 true。
DATABASE_ECHO=false

# 可选：runtime status snapshot 中暴露的版本号，默认 writerlab-v1。
# WRITERLAB_VERSION=writerlab-v1
